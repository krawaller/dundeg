import { EntityCollection } from '../interfaces'

const items: EntityCollection = {
  spikedgauntlet: {
    id: 'spikedgauntlet',
    type: 'item',
    hooks: {}
  },
  warpaint: {
    id: 'warpaint',
    type: 'item',
    hooks: {}
  }
};

export default items;