import {
  BattleState,
  HeroStatName,
  Attack,
  AttackOptions,
  ItemName,
  CalculationResult,
  HeroId,
} from '../interfaces'

interface BattleDiceNeedSpec {
  heroId: HeroId
}

export const find_hero_battle_dice_needs = (
  battle: BattleState,
  instr: BattleDiceNeedSpec
) => {
  const ret = {}
  return ret
}

// TODO - WIP
